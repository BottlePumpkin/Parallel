# Parallel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** macOS 앱 Parallel을 spec 그대로 구현 — Conductor형 worktree 매니저, worktree마다 PTY 세션 1개, git CLI만 호출(enterprise 호환).

**Architecture:** SwiftUI views + Services(WorktreeService, SessionManager, StatusWatcher) + WorkspaceStore(Codable JSON). PTY는 SwiftTerm + posix `forkpty`. Service/Util 레이어는 TDD(XCTest), Views/PTY는 manual test.

**Tech Stack:** Swift 5.10+, SwiftUI, macOS 14+, [SwiftTerm](https://github.com/migueldeicaza/SwiftTerm) (SPM), XCTest, `os.Logger`.

**Spec:** `docs/superpowers/specs/2026-05-27-parallel-design.md`

---

## File Structure

```
Parallel.xcodeproj
Parallel/
├─ ParallelApp.swift              # @main App entry
├─ Views/
│  ├─ ContentView.swift            # NavigationSplitView 컨테이너
│  ├─ SidebarView.swift            # repo 그룹 + worktree 리스트 + 배지
│  ├─ TerminalPaneView.swift       # 선택된 Session의 SwiftTerm 호스트
│  └─ Sheets/
│     ├─ AddRepoSheet.swift        # 폴더 선택 + 기존 worktree import
│     └─ NewWorktreeSheet.swift    # 브랜치·setup 입력 → worktree 생성
├─ Services/
│  ├─ GitCLI.swift                 # Process wrapper: exit code/stdout/stderr
│  ├─ WorktreeService.swift        # git worktree list/add/remove + status
│  ├─ PTY.swift                    # posix forkpty wrapper + I/O 스트림
│  ├─ SessionManager.swift         # worktree ↔ Session 매핑, lifecycle
│  └─ StatusWatcher.swift          # 5초 타이머, 동시 한도 4
├─ Models/
│  ├─ Repo.swift
│  ├─ Worktree.swift
│  ├─ WorktreeStatus.swift
│  └─ Session.swift
├─ Persistence/
│  └─ WorkspaceStore.swift         # workspace.json 영속화 + @Observable
└─ Util/
   ├─ PathSanitizer.swift          # branch → 디렉토리명 변환
   └─ AppLogger.swift              # os.Logger + 디버그 파일 로깅

ParallelTests/
├─ PathSanitizerTests.swift
├─ WorkspaceStoreTests.swift
├─ GitCLITests.swift
└─ WorktreeServiceTests.swift
```

**원칙:** 한 파일 한 책임. Service는 한 외부 시스템만(Worktree=git, Session=PTY). View는 상태 표시·이벤트만.

---

## Task 1: SPM 프로젝트 셋업·SwiftTerm 의존성 추가

> **Decision (2026-05-27):** Xcode `.xcodeproj` 대신 **Swift Package Manager 단독**. 이유: 환경 셋업을 자동화 가능, App Sandbox 처음부터 없음, 개인용이라 App Store 배포 불필요. Xcode IDE 경험은 `Package.swift`를 열면 동일.

**Files:**
- Create: `Package.swift`
- Create: `Sources/Parallel/ParallelApp.swift`
- Create: `Sources/Parallel/Views/ContentView.swift`
- Create: `Tests/ParallelTests/SmokeTests.swift`
- Create: `.gitignore`

- [ ] **Step 1: `Package.swift` 작성**

```swift
// swift-tools-version: 5.10
import PackageDescription

let package = Package(
    name: "Parallel",
    platforms: [.macOS(.v14)],
    products: [
        .executable(name: "Parallel", targets: ["Parallel"]),
    ],
    dependencies: [
        .package(url: "https://github.com/migueldeicaza/SwiftTerm", from: "1.2.0"),
    ],
    targets: [
        .executableTarget(
            name: "Parallel",
            dependencies: ["SwiftTerm"],
            path: "Sources/Parallel"
        ),
        .testTarget(
            name: "ParallelTests",
            dependencies: ["Parallel"],
            path: "Tests/ParallelTests"
        ),
    ]
)
```

- [ ] **Step 2: 최소 SwiftUI App**

`Sources/Parallel/ParallelApp.swift`:
```swift
import SwiftUI

@main
struct ParallelApp: App {
    var body: some Scene {
        WindowGroup("Parallel") {
            ContentView()
                .frame(minWidth: 900, minHeight: 600)
        }
    }
}
```

`Sources/Parallel/Views/ContentView.swift`:
```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Parallel")
            .padding()
    }
}
```

`Tests/ParallelTests/SmokeTests.swift`:
```swift
import XCTest
@testable import Parallel

final class SmokeTests: XCTestCase {
    func test_smoke() { XCTAssertTrue(true) }
}
```

- [ ] **Step 3: `.gitignore` 작성**

```gitignore
.build/
.swiftpm/
*.xcodeproj/
DerivedData/
.DS_Store
```

- [ ] **Step 4: 빌드·테스트 확인**

Run: `swift build` 그리고 `swift test`
Expected: 빌드 성공 + smoke test PASS.

- [ ] **Step 5: 실행 확인 (선택)**

Run: `swift run Parallel`
Expected: "Parallel" 텍스트 윈도우가 뜸. (또는 Xcode에서 `Package.swift` 열고 ⌘R)

- [ ] **Step 6: Commit**

```bash
git add Package.swift Package.resolved Sources/ Tests/ .gitignore
git commit -m "chore: scaffold Parallel via SwiftPM + SwiftTerm"
```

> **Note (디렉토리 구조):** plan의 §"File Structure" 섹션은 Xcode 구조 기준이라 `Parallel/`로 표기됐지만, SPM 구조는 `Sources/Parallel/`이 실제 경로. 이후 task의 `Create: Parallel/...`는 모두 `Sources/Parallel/...`로, `ParallelTests/...`는 `Tests/ParallelTests/...`로 읽어주세요.

---

## Task 2: PathSanitizer (TDD)

**Files:**
- Create: `Parallel/Util/PathSanitizer.swift`
- Test: `ParallelTests/PathSanitizerTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

`ParallelTests/PathSanitizerTests.swift`:
```swift
import XCTest
@testable import Parallel

final class PathSanitizerTests: XCTestCase {
    func test_replacesSlashesWithDash() {
        XCTAssertEqual(PathSanitizer.sanitize("feature/ISSUE-3625"), "feature-ISSUE-3625")
    }

    func test_collapsesWhitespace() {
        XCTAssertEqual(PathSanitizer.sanitize("feat  foo"), "feat-foo")
    }

    func test_stripsDangerousChars() {
        XCTAssertEqual(PathSanitizer.sanitize("feat:foo*bar?"), "feat-foo-bar")
    }

    func test_collapsesConsecutiveDashes() {
        XCTAssertEqual(PathSanitizer.sanitize("feat//bar"), "feat-bar")
    }

    func test_trimsLeadingTrailingDashes() {
        XCTAssertEqual(PathSanitizer.sanitize("/feat/"), "feat")
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: ⌘U
Expected: `PathSanitizer` 미정의로 빌드 실패.

- [ ] **Step 3: 최소 구현**

`Parallel/Util/PathSanitizer.swift`:
```swift
import Foundation

enum PathSanitizer {
    /// 브랜치명 등을 디렉토리 안전한 이름으로 변환.
    /// 규칙: 슬래시/공백/특수문자 → '-', 연속된 '-' 압축, 양끝 '-' 제거.
    static func sanitize(_ raw: String) -> String {
        let allowed = CharacterSet(charactersIn: "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789-_.")
        let replaced = raw.unicodeScalars.map { scalar -> Character in
            allowed.contains(scalar) ? Character(scalar) : "-"
        }
        let joined = String(replaced)
        let collapsed = joined.replacingOccurrences(of: "-+", with: "-", options: .regularExpression)
        return collapsed.trimmingCharacters(in: CharacterSet(charactersIn: "-"))
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: ⌘U
Expected: 5개 모두 PASS.

- [ ] **Step 5: Commit**

```bash
git add Parallel/Util/PathSanitizer.swift ParallelTests/PathSanitizerTests.swift
git commit -m "feat(util): PathSanitizer with TDD"
```

---

## Task 3: Model 타입 정의

**Files:**
- Create: `Parallel/Models/Repo.swift`
- Create: `Parallel/Models/Worktree.swift`
- Create: `Parallel/Models/WorktreeStatus.swift`
- Create: `Parallel/Models/Session.swift`

순수 데이터 타입. 직접 테스트는 다음 task(WorkspaceStore)에서 round-trip으로 검증.

- [ ] **Step 1: Repo struct**

`Parallel/Models/Repo.swift`:
```swift
import Foundation

struct Repo: Identifiable, Codable, Equatable, Hashable {
    let id: UUID
    var root: URL
    var displayName: String
    var worktreeBaseDir: String          // 기본 ".claude/worktrees", repo.root 기준 상대경로
    var defaultSetupCommands: [String]

    init(
        id: UUID = UUID(),
        root: URL,
        displayName: String,
        worktreeBaseDir: String = ".claude/worktrees",
        defaultSetupCommands: [String] = []
    ) {
        self.id = id
        self.root = root
        self.displayName = displayName
        self.worktreeBaseDir = worktreeBaseDir
        self.defaultSetupCommands = defaultSetupCommands
    }
}
```

- [ ] **Step 2: Worktree struct**

`Parallel/Models/Worktree.swift`:
```swift
import Foundation

struct Worktree: Identifiable, Codable, Equatable, Hashable {
    let id: UUID
    var repoId: UUID
    var path: URL
    var branch: String
    var displayName: String
    var createdAt: Date
    var lastUsedAt: Date
    var setupCommands: [String]

    init(
        id: UUID = UUID(),
        repoId: UUID,
        path: URL,
        branch: String,
        displayName: String,
        createdAt: Date = Date(),
        lastUsedAt: Date = Date(),
        setupCommands: [String] = []
    ) {
        self.id = id
        self.repoId = repoId
        self.path = path
        self.branch = branch
        self.displayName = displayName
        self.createdAt = createdAt
        self.lastUsedAt = lastUsedAt
        self.setupCommands = setupCommands
    }
}
```

- [ ] **Step 3: WorktreeStatus struct**

`Parallel/Models/WorktreeStatus.swift`:
```swift
import Foundation

struct WorktreeStatus: Equatable {
    var isDirty: Bool = false
    var changedFiles: Int = 0
    var ahead: Int = 0
    var behind: Int = 0
    var lastCheckedAt: Date? = nil
    var lastError: String? = nil
}
```

- [ ] **Step 4: Session class (영속화 X)**

`Parallel/Models/Session.swift`:
```swift
import Foundation

enum SessionState: Equatable {
    case running
    case exited(code: Int32)
}

/// SwiftTerm view를 SessionManager가 보유하므로 여기선 메타만.
final class Session: Identifiable, Equatable {
    let id: UUID
    let worktreeId: UUID
    var pid: pid_t
    var state: SessionState

    init(id: UUID = UUID(), worktreeId: UUID, pid: pid_t, state: SessionState = .running) {
        self.id = id
        self.worktreeId = worktreeId
        self.pid = pid
        self.state = state
    }

    static func == (lhs: Session, rhs: Session) -> Bool { lhs.id == rhs.id }
}
```

- [ ] **Step 5: 빌드 확인**

Run: ⌘B
Expected: 빌드 성공.

- [ ] **Step 6: Commit**

```bash
git add Parallel/Models/
git commit -m "feat(models): Repo/Worktree/WorktreeStatus/Session"
```

---

## Task 4: WorkspaceStore (TDD)

**Files:**
- Create: `Parallel/Persistence/WorkspaceStore.swift`
- Test: `ParallelTests/WorkspaceStoreTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

`ParallelTests/WorkspaceStoreTests.swift`:
```swift
import XCTest
@testable import Parallel

final class WorkspaceStoreTests: XCTestCase {
    var tempDir: URL!

    override func setUpWithError() throws {
        tempDir = URL(fileURLWithPath: NSTemporaryDirectory())
            .appendingPathComponent(UUID().uuidString)
        try FileManager.default.createDirectory(at: tempDir, withIntermediateDirectories: true)
    }

    override func tearDownWithError() throws {
        try? FileManager.default.removeItem(at: tempDir)
    }

    func test_initial_state_isEmpty() {
        let store = WorkspaceStore(directory: tempDir)
        XCTAssertTrue(store.repos.isEmpty)
        XCTAssertTrue(store.worktrees.isEmpty)
        XCTAssertNil(store.lastSelectedWorktreeId)
    }

    func test_saveAndLoad_roundTrip() throws {
        let store = WorkspaceStore(directory: tempDir)
        let repo = Repo(root: URL(fileURLWithPath: "/tmp/repo"), displayName: "demo")
        store.addRepo(repo)
        let worktree = Worktree(
            repoId: repo.id,
            path: URL(fileURLWithPath: "/tmp/repo/.claude/worktrees/feat-x"),
            branch: "feat/x",
            displayName: "feat-x"
        )
        store.addWorktree(worktree)
        store.lastSelectedWorktreeId = worktree.id
        try store.save()

        let reloaded = WorkspaceStore(directory: tempDir)
        try reloaded.load()
        XCTAssertEqual(reloaded.repos, [repo])
        XCTAssertEqual(reloaded.worktrees, [worktree])
        XCTAssertEqual(reloaded.lastSelectedWorktreeId, worktree.id)
    }

    func test_load_corruptedFile_quarantinesAndStartsEmpty() throws {
        let file = tempDir.appendingPathComponent("workspace.json")
        try "this is not json".data(using: .utf8)!.write(to: file)

        let store = WorkspaceStore(directory: tempDir)
        try store.load()
        XCTAssertTrue(store.repos.isEmpty)

        // 격리 파일이 만들어졌는지
        let files = try FileManager.default.contentsOfDirectory(atPath: tempDir.path)
        XCTAssertTrue(files.contains { $0.hasPrefix("workspace.json.corrupted-") })
    }

    func test_removeWorktree_removesIt() throws {
        let store = WorkspaceStore(directory: tempDir)
        let repo = Repo(root: URL(fileURLWithPath: "/tmp/r"), displayName: "r")
        store.addRepo(repo)
        let wt = Worktree(repoId: repo.id, path: URL(fileURLWithPath: "/tmp/r/.claude/worktrees/x"),
                          branch: "x", displayName: "x")
        store.addWorktree(wt)
        store.removeWorktree(id: wt.id)
        XCTAssertTrue(store.worktrees.isEmpty)
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: ⌘U
Expected: `WorkspaceStore` 미정의로 빌드 실패.

- [ ] **Step 3: 구현**

`Parallel/Persistence/WorkspaceStore.swift`:
```swift
import Foundation
import Observation

@Observable
final class WorkspaceStore {
    var repos: [Repo] = []
    var worktrees: [Worktree] = []
    var lastSelectedWorktreeId: UUID?

    private let directory: URL
    private let fileName = "workspace.json"

    init(directory: URL) {
        self.directory = directory
    }

    static var defaultDirectory: URL {
        let base = FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask)[0]
        return base.appendingPathComponent("Parallel", isDirectory: true)
    }

    private struct Payload: Codable {
        var version: Int
        var repos: [Repo]
        var worktrees: [Worktree]
        var lastSelectedWorktreeId: UUID?
    }

    private var fileURL: URL { directory.appendingPathComponent(fileName) }

    func load() throws {
        try FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
        guard FileManager.default.fileExists(atPath: fileURL.path) else { return }
        let data = try Data(contentsOf: fileURL)
        do {
            let payload = try JSONDecoder.iso.decode(Payload.self, from: data)
            self.repos = payload.repos
            self.worktrees = payload.worktrees
            self.lastSelectedWorktreeId = payload.lastSelectedWorktreeId
        } catch {
            // 손상 격리
            let stamp = Int(Date().timeIntervalSince1970)
            let quarantine = directory.appendingPathComponent("workspace.json.corrupted-\(stamp)")
            try? FileManager.default.moveItem(at: fileURL, to: quarantine)
            self.repos = []
            self.worktrees = []
            self.lastSelectedWorktreeId = nil
        }
    }

    func save() throws {
        try FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
        let payload = Payload(version: 1, repos: repos, worktrees: worktrees,
                              lastSelectedWorktreeId: lastSelectedWorktreeId)
        let data = try JSONEncoder.iso.encode(payload)
        try data.write(to: fileURL, options: .atomic)
    }

    func addRepo(_ repo: Repo) {
        repos.append(repo)
        try? save()
    }

    func addWorktree(_ wt: Worktree) {
        worktrees.append(wt)
        try? save()
    }

    func removeWorktree(id: UUID) {
        worktrees.removeAll { $0.id == id }
        if lastSelectedWorktreeId == id { lastSelectedWorktreeId = nil }
        try? save()
    }

    func removeRepo(id: UUID) {
        worktrees.removeAll { $0.repoId == id }
        repos.removeAll { $0.id == id }
        try? save()
    }

    func worktree(id: UUID) -> Worktree? {
        worktrees.first { $0.id == id }
    }
}

private extension JSONEncoder {
    static var iso: JSONEncoder {
        let e = JSONEncoder()
        e.dateEncodingStrategy = .iso8601
        e.outputFormatting = [.prettyPrinted, .sortedKeys]
        return e
    }
}

private extension JSONDecoder {
    static var iso: JSONDecoder {
        let d = JSONDecoder()
        d.dateDecodingStrategy = .iso8601
        return d
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: ⌘U
Expected: WorkspaceStoreTests 4개 모두 PASS.

- [ ] **Step 5: Commit**

```bash
git add Parallel/Persistence/WorkspaceStore.swift ParallelTests/WorkspaceStoreTests.swift
git commit -m "feat(persistence): WorkspaceStore with corruption quarantine"
```

---

## Task 5: GitCLI wrapper (TDD)

**Files:**
- Create: `Parallel/Services/GitCLI.swift`
- Test: `ParallelTests/GitCLITests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

`ParallelTests/GitCLITests.swift`:
```swift
import XCTest
@testable import Parallel

final class GitCLITests: XCTestCase {
    var repoRoot: URL!

    override func setUpWithError() throws {
        let tmp = URL(fileURLWithPath: NSTemporaryDirectory())
            .appendingPathComponent(UUID().uuidString)
        try FileManager.default.createDirectory(at: tmp, withIntermediateDirectories: true)
        repoRoot = tmp
        // init + 초기 커밋
        _ = try GitCLI.run(["init", "-q", "-b", "main"], in: tmp)
        _ = try GitCLI.run(["config", "user.email", "t@t"], in: tmp)
        _ = try GitCLI.run(["config", "user.name", "t"], in: tmp)
        try "hi".write(to: tmp.appendingPathComponent("a.txt"), atomically: true, encoding: .utf8)
        _ = try GitCLI.run(["add", "."], in: tmp)
        _ = try GitCLI.run(["commit", "-qm", "init"], in: tmp)
    }

    override func tearDownWithError() throws {
        try? FileManager.default.removeItem(at: repoRoot)
    }

    func test_run_success_returnsStdout() throws {
        let r = try GitCLI.run(["rev-parse", "--abbrev-ref", "HEAD"], in: repoRoot)
        XCTAssertEqual(r.exitCode, 0)
        XCTAssertEqual(r.stdout.trimmingCharacters(in: .whitespacesAndNewlines), "main")
    }

    func test_run_failure_capturesStderr() throws {
        let r = try GitCLI.run(["nonsense-subcommand"], in: repoRoot)
        XCTAssertNotEqual(r.exitCode, 0)
        XCTAssertFalse(r.stderr.isEmpty)
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: ⌘U
Expected: `GitCLI` 미정의로 빌드 실패.

- [ ] **Step 3: 구현**

`Parallel/Services/GitCLI.swift`:
```swift
import Foundation

enum GitCLI {
    struct Result {
        let exitCode: Int32
        let stdout: String
        let stderr: String
    }

    enum GitError: Error, CustomStringConvertible {
        case launchFailed(String)
        var description: String {
            switch self {
            case .launchFailed(let m): return "git launch failed: \(m)"
            }
        }
    }

    /// 동기 실행. 실패 시 GitError. 종료코드 ≠ 0은 정상 반환 (caller가 판단).
    static func run(_ args: [String], in cwd: URL, timeout: TimeInterval = 30) throws -> Result {
        let proc = Process()
        proc.executableURL = URL(fileURLWithPath: "/usr/bin/env")
        proc.arguments = ["git"] + args
        proc.currentDirectoryURL = cwd

        let outPipe = Pipe()
        let errPipe = Pipe()
        proc.standardOutput = outPipe
        proc.standardError = errPipe

        do {
            try proc.run()
        } catch {
            throw GitError.launchFailed("\(error)")
        }

        proc.waitUntilExit()

        let outData = outPipe.fileHandleForReading.readDataToEndOfFile()
        let errData = errPipe.fileHandleForReading.readDataToEndOfFile()

        return Result(
            exitCode: proc.terminationStatus,
            stdout: String(data: outData, encoding: .utf8) ?? "",
            stderr: String(data: errData, encoding: .utf8) ?? ""
        )
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: ⌘U
Expected: GitCLITests 2개 PASS.

- [ ] **Step 5: Commit**

```bash
git add Parallel/Services/GitCLI.swift ParallelTests/GitCLITests.swift
git commit -m "feat(services): GitCLI Process wrapper with stdout/stderr capture"
```

---

## Task 6: WorktreeService.list (TDD)

**Files:**
- Create: `Parallel/Services/WorktreeService.swift`
- Test: `ParallelTests/WorktreeServiceTests.swift`

- [ ] **Step 1: 실패하는 테스트 작성**

`ParallelTests/WorktreeServiceTests.swift`:
```swift
import XCTest
@testable import Parallel

final class WorktreeServiceTests: XCTestCase {
    var repoRoot: URL!

    override func setUpWithError() throws {
        let tmp = URL(fileURLWithPath: NSTemporaryDirectory())
            .appendingPathComponent(UUID().uuidString)
        try FileManager.default.createDirectory(at: tmp, withIntermediateDirectories: true)
        repoRoot = tmp
        _ = try GitCLI.run(["init", "-q", "-b", "main"], in: tmp)
        _ = try GitCLI.run(["config", "user.email", "t@t"], in: tmp)
        _ = try GitCLI.run(["config", "user.name", "t"], in: tmp)
        try "hi".write(to: tmp.appendingPathComponent("a.txt"), atomically: true, encoding: .utf8)
        _ = try GitCLI.run(["add", "."], in: tmp)
        _ = try GitCLI.run(["commit", "-qm", "init"], in: tmp)
    }

    override func tearDownWithError() throws {
        try? FileManager.default.removeItem(at: repoRoot)
    }

    func test_list_freshRepo_returnsMainWorktree() throws {
        let svc = WorktreeService()
        let entries = try svc.list(in: repoRoot)
        XCTAssertEqual(entries.count, 1)
        XCTAssertEqual(entries[0].branch, "main")
        XCTAssertEqual(entries[0].path.standardizedFileURL, repoRoot.standardizedFileURL)
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: ⌘U
Expected: `WorktreeService` 미정의로 빌드 실패.

- [ ] **Step 3: 구현**

`Parallel/Services/WorktreeService.swift`:
```swift
import Foundation

final class WorktreeService {

    struct Entry: Equatable {
        var path: URL
        var branch: String
        var head: String       // commit sha
    }

    enum ServiceError: LocalizedError {
        case gitFailed(stderr: String, exitCode: Int32)
        var errorDescription: String? {
            switch self {
            case .gitFailed(let s, let c): return "git failed (exit \(c)): \(s)"
            }
        }
    }

    func list(in repoRoot: URL) throws -> [Entry] {
        let r = try GitCLI.run(["worktree", "list", "--porcelain"], in: repoRoot)
        guard r.exitCode == 0 else {
            throw ServiceError.gitFailed(stderr: r.stderr, exitCode: r.exitCode)
        }
        return Self.parseList(r.stdout)
    }

    /// `git worktree list --porcelain` 블록 파서.
    /// 각 블록은 빈 줄로 구분. `worktree <path>`, `HEAD <sha>`, `branch refs/heads/<name>` 또는 `detached`.
    static func parseList(_ raw: String) -> [Entry] {
        var entries: [Entry] = []
        let blocks = raw.components(separatedBy: "\n\n")
        for block in blocks {
            var path: URL?
            var branch: String?
            var head: String?
            for line in block.split(separator: "\n", omittingEmptySubsequences: true) {
                let s = String(line)
                if s.hasPrefix("worktree ") {
                    path = URL(fileURLWithPath: String(s.dropFirst("worktree ".count)))
                } else if s.hasPrefix("HEAD ") {
                    head = String(s.dropFirst("HEAD ".count))
                } else if s.hasPrefix("branch refs/heads/") {
                    branch = String(s.dropFirst("branch refs/heads/".count))
                } else if s == "detached" {
                    branch = "(detached)"
                }
            }
            if let path, let head {
                entries.append(Entry(path: path, branch: branch ?? "(unknown)", head: head))
            }
        }
        return entries
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: ⌘U
Expected: WorktreeServiceTests PASS.

- [ ] **Step 5: Commit**

```bash
git add Parallel/Services/WorktreeService.swift ParallelTests/WorktreeServiceTests.swift
git commit -m "feat(services): WorktreeService.list with porcelain parser"
```

---

## Task 7: WorktreeService.add (TDD)

**Files:**
- Modify: `Parallel/Services/WorktreeService.swift`
- Modify: `ParallelTests/WorktreeServiceTests.swift`

- [ ] **Step 1: 실패 테스트 추가**

`WorktreeServiceTests.swift`에 추가:
```swift
func test_add_createsNewBranchAndWorktree() throws {
    let svc = WorktreeService()
    let dest = repoRoot.appendingPathComponent(".claude/worktrees/feat-x")
    try svc.add(repoRoot: repoRoot, branch: "feat/x", base: "main", path: dest, createBranch: true)

    let entries = try svc.list(in: repoRoot)
    XCTAssertEqual(entries.count, 2)
    XCTAssertTrue(entries.contains { $0.branch == "feat/x" })
}

func test_add_existingPath_throws() throws {
    let svc = WorktreeService()
    let dest = repoRoot.appendingPathComponent(".claude/worktrees/feat-y")
    try svc.add(repoRoot: repoRoot, branch: "feat/y", base: "main", path: dest, createBranch: true)
    XCTAssertThrowsError(try svc.add(repoRoot: repoRoot, branch: "feat/y", base: "main", path: dest, createBranch: true))
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: ⌘U
Expected: `add` 메서드 미정의로 빌드 실패.

- [ ] **Step 3: 구현 추가**

`WorktreeService.swift`에 추가:
```swift
extension WorktreeService {
    /// `git worktree add` 실행.
    /// - createBranch=true: `-b <branch> <path> <base>`
    /// - createBranch=false: `<path> <branch>` (기존 브랜치 체크아웃)
    func add(repoRoot: URL, branch: String, base: String, path: URL, createBranch: Bool) throws {
        // 부모 디렉토리 보장
        try FileManager.default.createDirectory(
            at: path.deletingLastPathComponent(),
            withIntermediateDirectories: true
        )
        let args: [String]
        if createBranch {
            args = ["worktree", "add", "-b", branch, path.path, base]
        } else {
            args = ["worktree", "add", path.path, branch]
        }
        let r = try GitCLI.run(args, in: repoRoot)
        guard r.exitCode == 0 else {
            throw ServiceError.gitFailed(stderr: r.stderr, exitCode: r.exitCode)
        }
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: ⌘U
Expected: 새 테스트 2개 PASS.

- [ ] **Step 5: Commit**

```bash
git add Parallel/Services/WorktreeService.swift ParallelTests/WorktreeServiceTests.swift
git commit -m "feat(services): WorktreeService.add"
```

---

## Task 8: WorktreeService.remove + status (TDD)

**Files:**
- Modify: `Parallel/Services/WorktreeService.swift`
- Modify: `ParallelTests/WorktreeServiceTests.swift`

- [ ] **Step 1: 실패 테스트 추가**

```swift
func test_remove_deletesWorktree() throws {
    let svc = WorktreeService()
    let dest = repoRoot.appendingPathComponent(".claude/worktrees/feat-r")
    try svc.add(repoRoot: repoRoot, branch: "feat/r", base: "main", path: dest, createBranch: true)
    try svc.remove(repoRoot: repoRoot, path: dest, force: false)
    let entries = try svc.list(in: repoRoot)
    XCTAssertEqual(entries.count, 1)
}

func test_status_cleanWorktree() throws {
    let svc = WorktreeService()
    let s = try svc.status(at: repoRoot)
    XCTAssertFalse(s.isDirty)
    XCTAssertEqual(s.changedFiles, 0)
}

func test_status_dirtyWorktree() throws {
    try "changed".write(to: repoRoot.appendingPathComponent("b.txt"),
                        atomically: true, encoding: .utf8)
    let svc = WorktreeService()
    let s = try svc.status(at: repoRoot)
    XCTAssertTrue(s.isDirty)
    XCTAssertEqual(s.changedFiles, 1)
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: ⌘U
Expected: `remove`, `status` 미정의로 빌드 실패.

- [ ] **Step 3: 구현 추가**

`WorktreeService.swift`에 추가:
```swift
extension WorktreeService {
    func remove(repoRoot: URL, path: URL, force: Bool) throws {
        var args = ["worktree", "remove"]
        if force { args.append("--force") }
        args.append(path.path)
        let r = try GitCLI.run(args, in: repoRoot)
        guard r.exitCode == 0 else {
            throw ServiceError.gitFailed(stderr: r.stderr, exitCode: r.exitCode)
        }
    }

    /// `git status --porcelain` + ahead/behind 정보를 모아서 WorktreeStatus 반환.
    func status(at path: URL) throws -> WorktreeStatus {
        var out = WorktreeStatus(lastCheckedAt: Date())

        let porcelain = try GitCLI.run(["status", "--porcelain"], in: path)
        guard porcelain.exitCode == 0 else {
            throw ServiceError.gitFailed(stderr: porcelain.stderr, exitCode: porcelain.exitCode)
        }
        let lines = porcelain.stdout.split(separator: "\n", omittingEmptySubsequences: true)
        out.changedFiles = lines.count
        out.isDirty = !lines.isEmpty

        let counts = try GitCLI.run(
            ["rev-list", "--left-right", "--count", "HEAD...@{upstream}"],
            in: path
        )
        if counts.exitCode == 0 {
            let parts = counts.stdout
                .trimmingCharacters(in: .whitespacesAndNewlines)
                .split(separator: "\t")
            if parts.count == 2,
               let ahead = Int(parts[0]),
               let behind = Int(parts[1]) {
                out.ahead = ahead
                out.behind = behind
            }
        }
        return out
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: ⌘U
Expected: 새 테스트 3개 PASS.

- [ ] **Step 5: Commit**

```bash
git add Parallel/Services/WorktreeService.swift ParallelTests/WorktreeServiceTests.swift
git commit -m "feat(services): WorktreeService.remove + status"
```

---

## Task 9: PTY wrapper (manual test)

PTY는 시간·시스템 의존이 커서 단위 테스트 대신 수동 검증. 그러나 SessionManager에서 쓰이는 작은 표면을 갖추는 게 목적.

**Files:**
- Create: `Parallel/Services/PTY.swift`

- [ ] **Step 1: 구현**

`Parallel/Services/PTY.swift`:
```swift
import Foundation
import Darwin

/// posix forkpty wrapper.
/// 자식 프로세스로 셸을 띄우고, master fd로 입출력.
final class PTY {

    let pid: pid_t
    let masterFD: Int32

    /// 셸 fork. 실패 시 nil.
    /// - Parameters:
    ///   - shell: 실행할 셸 path. 보통 $SHELL 또는 /bin/zsh.
    ///   - cwd: 자식의 작업 디렉토리.
    ///   - env: 환경변수 (없으면 부모 환경 상속).
    ///   - cols/rows: 초기 터미널 크기.
    init?(shell: String, cwd: URL, env: [String: String] = ProcessInfo.processInfo.environment,
          cols: Int32 = 120, rows: Int32 = 32) {
        var master: Int32 = 0
        var size = winsize(ws_row: UInt16(rows), ws_col: UInt16(cols), ws_xpixel: 0, ws_ypixel: 0)

        let pid = withUnsafeMutablePointer(to: &size) { sizePtr -> pid_t in
            forkpty(&master, nil, nil, sizePtr)
        }

        guard pid >= 0 else { return nil }

        if pid == 0 {
            // child
            chdir(cwd.path)
            // TERM 보장
            var mergedEnv = env
            mergedEnv["TERM"] = mergedEnv["TERM"] ?? "xterm-256color"
            let cEnv = mergedEnv.map { strdup("\($0.key)=\($0.value)") } + [nil]
            let cArgs: [UnsafeMutablePointer<CChar>?] = [strdup(shell), nil]
            execve(shell, cArgs, cEnv)
            exit(127) // exec 실패
        }

        self.pid = pid
        self.masterFD = master
    }

    /// stdin 쓰기.
    func write(_ data: Data) {
        data.withUnsafeBytes { _ = Darwin.write(masterFD, $0.baseAddress, data.count) }
    }

    func resize(cols: Int32, rows: Int32) {
        var size = winsize(ws_row: UInt16(rows), ws_col: UInt16(cols), ws_xpixel: 0, ws_ypixel: 0)
        _ = withUnsafeMutablePointer(to: &size) { ioctl(masterFD, TIOCSWINSZ, $0) }
    }

    /// stdout/stderr 비동기 읽기 — 콜백으로 데이터 전달.
    /// dispatch source를 이용. 호출자가 stop을 책임.
    func startReading(onData: @escaping (Data) -> Void, onEOF: @escaping () -> Void) -> DispatchSourceRead {
        let source = DispatchSource.makeReadSource(fileDescriptor: masterFD, queue: .global(qos: .userInitiated))
        source.setEventHandler {
            let avail = Int(source.data)
            guard avail > 0 else { return }
            var buf = [UInt8](repeating: 0, count: avail)
            let n = read(self.masterFD, &buf, avail)
            if n > 0 {
                onData(Data(buf.prefix(n)))
            } else if n == 0 {
                onEOF()
                source.cancel()
            }
        }
        source.resume()
        return source
    }

    /// 자식 프로세스 종료. SIGTERM → 2초 후 SIGKILL.
    func terminate() {
        kill(pid, SIGTERM)
        DispatchQueue.global().asyncAfter(deadline: .now() + 2) {
            kill(self.pid, SIGKILL)
        }
    }
}
```

- [ ] **Step 2: 빌드 확인**

Run: ⌘B
Expected: 빌드 성공.

- [ ] **Step 3: 수동 spike**

`ContentView.swift`를 임시로 수정:
```swift
import SwiftUI
import SwiftTerm

struct ContentView: View {
    var body: some View {
        TerminalSpikeView()
            .frame(minWidth: 800, minHeight: 500)
    }
}

struct TerminalSpikeView: NSViewRepresentable {
    func makeNSView(context: Context) -> LocalProcessTerminalView {
        let view = LocalProcessTerminalView(frame: .zero)
        let shell = ProcessInfo.processInfo.environment["SHELL"] ?? "/bin/zsh"
        view.startProcess(executable: shell, args: [], environment: nil, execName: "-zsh")
        return view
    }
    func updateNSView(_ nsView: LocalProcessTerminalView, context: Context) {}
}
```

Run ⌘R → 윈도우에 셸 프롬프트가 떠야 함. `ls`·`echo hi` 입력해서 동작 확인.

- [ ] **Step 4: ContentView 원복 + Commit**

`ContentView`는 원래 빈 텍스트로 되돌리기.
```bash
git checkout -- Parallel/Views/ContentView.swift
git add Parallel/Services/PTY.swift
git commit -m "feat(services): PTY (posix forkpty) wrapper, spike confirmed"
```

> **Note:** Task 13(TerminalPaneView)에서 SwiftTerm의 `LocalProcessTerminalView` 또는 우리 PTY+`TerminalView`를 어느 쪽으로 갈지 결정. MVP는 `LocalProcessTerminalView`를 쓰면 PTY.swift는 사실상 미사용이지만, setup 명령 자동 입력·세션 lifecycle 통제를 위해 우리 PTY 쓰는 방향이 더 깔끔. Task 13에서 본격 통합.

---

## Task 10: SessionManager (manual test)

**Files:**
- Create: `Parallel/Services/SessionManager.swift`

- [ ] **Step 1: 구현**

`Parallel/Services/SessionManager.swift`:
```swift
import Foundation
import SwiftTerm
import Observation
import AppKit

@Observable
final class SessionManager {

    /// worktreeId → 세션 보유 정보
    private var sessions: [UUID: SessionEntry] = [:]

    struct SessionEntry {
        let session: Session
        let pty: PTY
        let terminalView: TerminalView   // SwiftTerm UI
        var readSource: DispatchSourceRead?
        var pendingSetupCommands: [String]
    }

    /// 세션을 가져오거나(있으면) 만든다.
    @discardableResult
    func ensureSession(for worktree: Worktree, setupCommands: [String] = []) -> SessionEntry? {
        if let e = sessions[worktree.id] { return e }
        return startSession(for: worktree, setupCommands: setupCommands)
    }

    /// 새 세션 시작.
    @discardableResult
    func startSession(for worktree: Worktree, setupCommands: [String]) -> SessionEntry? {
        let shell = ProcessInfo.processInfo.environment["SHELL"] ?? "/bin/zsh"
        guard let pty = PTY(shell: shell, cwd: worktree.path) else { return nil }

        let view = TerminalView(frame: NSRect(x: 0, y: 0, width: 800, height: 500))
        view.terminalDelegate = SessionTerminalDelegate(pty: pty)

        var entry = SessionEntry(
            session: Session(worktreeId: worktree.id, pid: pty.pid),
            pty: pty,
            terminalView: view,
            readSource: nil,
            pendingSetupCommands: setupCommands
        )

        entry.readSource = pty.startReading(
            onData: { data in
                DispatchQueue.main.async {
                    let bytes = [UInt8](data)
                    view.feed(byteArray: ArraySlice(bytes))
                }
            },
            onEOF: { [weak self] in
                DispatchQueue.main.async {
                    self?.markExited(worktreeId: worktree.id)
                }
            }
        )

        sessions[worktree.id] = entry

        // setup 명령 자동 입력 — 500ms 지연 후 enqueue
        if !setupCommands.isEmpty {
            DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) { [weak self] in
                self?.flushSetupCommands(worktreeId: worktree.id)
            }
        }

        return entry
    }

    func session(for worktreeId: UUID) -> SessionEntry? {
        sessions[worktreeId]
    }

    func terminate(worktreeId: UUID) {
        guard let entry = sessions[worktreeId] else { return }
        entry.pty.terminate()
        entry.readSource?.cancel()
        sessions.removeValue(forKey: worktreeId)
    }

    func restartSession(for worktree: Worktree, setupCommands: [String]) {
        terminate(worktreeId: worktree.id)
        _ = startSession(for: worktree, setupCommands: setupCommands)
    }

    private func markExited(worktreeId: UUID) {
        guard var e = sessions[worktreeId] else { return }
        e.session.state = .exited(code: 0)
        sessions[worktreeId] = e
    }

    private func flushSetupCommands(worktreeId: UUID) {
        guard var e = sessions[worktreeId] else { return }
        for cmd in e.pendingSetupCommands {
            let line = cmd + "\n"
            if let data = line.data(using: .utf8) {
                e.pty.write(data)
            }
        }
        e.pendingSetupCommands = []
        sessions[worktreeId] = e
    }
}

private final class SessionTerminalDelegate: NSObject, TerminalViewDelegate {
    let pty: PTY
    init(pty: PTY) { self.pty = pty }

    func send(source: TerminalView, data: ArraySlice<UInt8>) {
        pty.write(Data(data))
    }
    func scrolled(source: TerminalView, position: Double) {}
    func setTerminalTitle(source: TerminalView, title: String) {}
    func sizeChanged(source: TerminalView, newCols: Int, newRows: Int) {
        pty.resize(cols: Int32(newCols), rows: Int32(newRows))
    }
    func hostCurrentDirectoryUpdate(source: TerminalView, directory: String?) {}
    func bell(source: TerminalView) {}
    func clipboardCopy(source: TerminalView, content: Data) {}
    func rangeChanged(source: TerminalView, startY: Int, endY: Int) {}
    func iTermContent(source: TerminalView, content: ArraySlice<UInt8>) {}
    func requestOpenLink(source: TerminalView, link: String, params: [String : String]) {
        guard let url = URL(string: link) else { return }
        NSWorkspace.shared.open(url)
    }
}
```

- [ ] **Step 2: 빌드 확인**

Run: ⌘B
Expected: 성공.

- [ ] **Step 3: Commit**

```bash
git add Parallel/Services/SessionManager.swift
git commit -m "feat(services): SessionManager with PTY+SwiftTerm + setup command queue"
```

---

## Task 11: ContentView 골격 + 환경 의존성 주입

**Files:**
- Modify: `Parallel/ParallelApp.swift`
- Modify: `Parallel/Views/ContentView.swift`

- [ ] **Step 1: App 진입점에서 store·session manager 보유**

`ParallelApp.swift`:
```swift
import SwiftUI

@main
struct ParallelApp: App {
    @State private var store: WorkspaceStore = {
        let s = WorkspaceStore(directory: WorkspaceStore.defaultDirectory)
        try? s.load()
        return s
    }()
    @State private var sessionManager = SessionManager()

    var body: some Scene {
        WindowGroup("Parallel") {
            ContentView()
                .frame(minWidth: 900, minHeight: 600)
                .environment(store)
                .environment(sessionManager)
        }
        .windowResizability(.contentSize)
    }
}
```

- [ ] **Step 2: ContentView NavigationSplitView**

`Views/ContentView.swift`:
```swift
import SwiftUI

struct ContentView: View {
    @Environment(WorkspaceStore.self) private var store
    @State private var selectedWorktreeId: UUID?

    var body: some View {
        @Bindable var bindableStore = store
        NavigationSplitView {
            SidebarView(selection: $selectedWorktreeId)
                .navigationSplitViewColumnWidth(min: 200, ideal: 240, max: 320)
        } detail: {
            TerminalPaneView(worktreeId: selectedWorktreeId)
        }
        .onAppear {
            if selectedWorktreeId == nil {
                selectedWorktreeId = store.lastSelectedWorktreeId
            }
        }
        .onChange(of: selectedWorktreeId) { _, new in
            store.lastSelectedWorktreeId = new
            try? store.save()
        }
    }
}
```

- [ ] **Step 3: 빈 SidebarView/TerminalPaneView 스텁**

`Views/SidebarView.swift`:
```swift
import SwiftUI

struct SidebarView: View {
    @Binding var selection: UUID?

    var body: some View {
        List(selection: $selection) {
            Text("(empty — add a repository)")
                .foregroundStyle(.secondary)
        }
    }
}
```

`Views/TerminalPaneView.swift`:
```swift
import SwiftUI

struct TerminalPaneView: View {
    let worktreeId: UUID?

    var body: some View {
        VStack {
            Spacer()
            Text(worktreeId == nil ? "Select a worktree" : "Terminal goes here")
                .foregroundStyle(.secondary)
            Spacer()
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
```

- [ ] **Step 4: 빌드·실행 확인**

Run: ⌘R
Expected: 사이드바 "(empty)" + 디테일 "Select a worktree".

- [ ] **Step 5: Commit**

```bash
git add Parallel/ParallelApp.swift Parallel/Views/
git commit -m "feat(views): NavigationSplitView skeleton with environment injection"
```

---

## Task 12: SidebarView — repo 그룹 + worktree 리스트

**Files:**
- Modify: `Parallel/Views/SidebarView.swift`

- [ ] **Step 1: 구현**

`Views/SidebarView.swift`:
```swift
import SwiftUI

struct SidebarView: View {
    @Environment(WorkspaceStore.self) private var store
    @Binding var selection: UUID?

    var body: some View {
        List(selection: $selection) {
            ForEach(store.repos) { repo in
                Section(repo.displayName) {
                    let items = store.worktrees.filter { $0.repoId == repo.id }
                    if items.isEmpty {
                        Text("(no worktrees)")
                            .foregroundStyle(.secondary)
                            .font(.caption)
                    } else {
                        ForEach(items) { wt in
                            WorktreeRow(worktree: wt)
                                .tag(wt.id as UUID?)
                        }
                    }
                }
            }
            if store.repos.isEmpty {
                Text("(no repositories)")
                    .foregroundStyle(.secondary)
            }
        }
        .listStyle(.sidebar)
    }
}

private struct WorktreeRow: View {
    let worktree: Worktree

    var body: some View {
        HStack(spacing: 6) {
            Circle().frame(width: 6, height: 6).foregroundStyle(.tertiary)  // 상태 배지 — Task 18에서 교체
            Text(worktree.displayName)
                .lineLimit(1)
            Spacer()
        }
    }
}
```

- [ ] **Step 2: 임시 데이터로 수동 확인**

`ParallelApp.swift` `onAppear`에 임시 mock 데이터 주입(나중에 제거):
```swift
// 디버그용 — Task 13에서 제거
.task {
    if store.repos.isEmpty {
        let r = Repo(root: URL(fileURLWithPath: "/tmp/demo"), displayName: "demo")
        store.addRepo(r)
        store.addWorktree(Worktree(repoId: r.id,
                                   path: URL(fileURLWithPath: "/tmp/demo/wt1"),
                                   branch: "feat/x", displayName: "feat-x"))
    }
}
```
(ContentView의 body에 `.task { ... }` 형태로)

Run: ⌘R → "demo" 그룹 아래 "feat-x" 행이 보여야 함.

- [ ] **Step 3: 임시 mock 제거 + Commit**

mock 코드 제거 후:
```bash
git add Parallel/Views/SidebarView.swift Parallel/Views/ContentView.swift
git commit -m "feat(views): SidebarView with repo grouping and worktree rows"
```

---

## Task 13: TerminalPaneView — SwiftTerm 호스트

**Files:**
- Modify: `Parallel/Views/TerminalPaneView.swift`

- [ ] **Step 1: 구현**

`Views/TerminalPaneView.swift`:
```swift
import SwiftUI
import SwiftTerm
import AppKit

struct TerminalPaneView: View {
    @Environment(WorkspaceStore.self) private var store
    @Environment(SessionManager.self) private var sessionManager
    let worktreeId: UUID?

    var body: some View {
        Group {
            if let id = worktreeId, let wt = store.worktree(id: id) {
                TerminalContainer(worktree: wt)
                    .id(wt.id)   // 다른 worktree로 바뀌면 view 교체
            } else {
                placeholder
            }
        }
    }

    private var placeholder: some View {
        VStack(spacing: 8) {
            Image(systemName: "terminal")
                .font(.system(size: 40))
                .foregroundStyle(.tertiary)
            Text(worktreeId == nil ? "Select a worktree" : "No session")
                .foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

/// SessionManager로부터 TerminalView를 가져와 NSViewRepresentable로 wrap.
private struct TerminalContainer: NSViewRepresentable {
    @Environment(SessionManager.self) private var sessionManager
    let worktree: Worktree

    func makeNSView(context: Context) -> TerminalView {
        let entry = sessionManager.ensureSession(for: worktree, setupCommands: worktree.setupCommands)
        return entry?.terminalView ?? TerminalView(frame: .zero)
    }

    func updateNSView(_ nsView: TerminalView, context: Context) {
        // 세션이 죽었거나 worktreeId가 바뀌면 .id() modifier가 재생성
    }
}
```

- [ ] **Step 2: 빌드 확인**

Run: ⌘B
Expected: 성공. 실행해도 worktree가 없으니 placeholder만 보임.

- [ ] **Step 3: Commit**

```bash
git add Parallel/Views/TerminalPaneView.swift
git commit -m "feat(views): TerminalPaneView hosting SwiftTerm via SessionManager"
```

---

## Task 14: AddRepoSheet — 폴더 선택 + 기존 worktree import

**Files:**
- Create: `Parallel/Views/Sheets/AddRepoSheet.swift`
- Modify: `Parallel/Views/ContentView.swift` (toolbar + sheet presentation)

- [ ] **Step 1: AddRepoSheet 구현**

`Views/Sheets/AddRepoSheet.swift`:
```swift
import SwiftUI
import AppKit

struct AddRepoSheet: View {
    @Environment(WorkspaceStore.self) private var store
    @Environment(SessionManager.self) private var sessionManager
    @Environment(\.dismiss) private var dismiss

    @State private var selectedRoot: URL?
    @State private var displayName = ""
    @State private var worktreeBaseDir = ".claude/worktrees"
    @State private var defaultSetupCommands = ""
    @State private var discovered: [WorktreeService.Entry] = []
    @State private var importChoices: [Bool] = []
    @State private var errorMessage: String?

    private let svc = WorktreeService()

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Add Repository").font(.title2).bold()

            HStack {
                Text(selectedRoot?.path ?? "(no folder)")
                    .lineLimit(1)
                    .truncationMode(.middle)
                Spacer()
                Button("Choose…") { pickFolder() }
            }

            TextField("Display name", text: $displayName)
            TextField("Worktree base dir (relative to repo root)", text: $worktreeBaseDir)
            TextField("Default setup commands (one per line)",
                      text: $defaultSetupCommands, axis: .vertical)
                .lineLimit(3, reservesSpace: true)

            if !discovered.isEmpty {
                Text("Existing worktrees (select to import):").font(.headline)
                List {
                    ForEach(Array(discovered.enumerated()), id: \.offset) { idx, e in
                        Toggle(isOn: Binding(
                            get: { importChoices[idx] },
                            set: { importChoices[idx] = $0 }
                        )) {
                            VStack(alignment: .leading) {
                                Text(e.branch).font(.body)
                                Text(e.path.path).font(.caption).foregroundStyle(.secondary)
                            }
                        }
                    }
                }
                .frame(minHeight: 120, maxHeight: 200)
            }

            if let errorMessage {
                Text(errorMessage).foregroundStyle(.red)
            }

            HStack {
                Spacer()
                Button("Cancel") { dismiss() }
                Button("Add") { commit() }
                    .keyboardShortcut(.defaultAction)
                    .disabled(selectedRoot == nil || displayName.isEmpty)
            }
        }
        .padding(20)
        .frame(width: 480)
    }

    private func pickFolder() {
        let panel = NSOpenPanel()
        panel.canChooseDirectories = true
        panel.canChooseFiles = false
        panel.allowsMultipleSelection = false
        if panel.runModal() == .OK, let url = panel.url {
            selectedRoot = url
            if displayName.isEmpty { displayName = url.lastPathComponent }
            do {
                discovered = try svc.list(in: url)
                importChoices = Array(repeating: false, count: discovered.count)
            } catch {
                errorMessage = "Failed to scan worktrees: \(error.localizedDescription)"
            }
        }
    }

    private func commit() {
        guard let root = selectedRoot else { return }
        let setupLines = defaultSetupCommands
            .split(separator: "\n").map { String($0).trimmingCharacters(in: .whitespaces) }
            .filter { !$0.isEmpty }
        let repo = Repo(root: root, displayName: displayName,
                        worktreeBaseDir: worktreeBaseDir,
                        defaultSetupCommands: setupLines)
        store.addRepo(repo)

        // 메인 worktree(repo.root)는 import 대상에서 제외하고, 나머지 선택된 것만 추가
        for (idx, entry) in discovered.enumerated() where importChoices[idx] {
            if entry.path.standardizedFileURL == root.standardizedFileURL { continue }
            let wt = Worktree(
                repoId: repo.id,
                path: entry.path,
                branch: entry.branch,
                displayName: entry.path.lastPathComponent,
                setupCommands: setupLines
            )
            store.addWorktree(wt)
            sessionManager.ensureSession(for: wt, setupCommands: setupLines)
        }
        dismiss()
    }
}
```

- [ ] **Step 2: ContentView toolbar + 시트 표시**

`Views/ContentView.swift`에 state·toolbar 추가:
```swift
struct ContentView: View {
    @Environment(WorkspaceStore.self) private var store
    @State private var selectedWorktreeId: UUID?
    @State private var showAddRepo = false

    var body: some View {
        NavigationSplitView {
            SidebarView(selection: $selectedWorktreeId)
                .navigationSplitViewColumnWidth(min: 200, ideal: 240, max: 320)
        } detail: {
            TerminalPaneView(worktreeId: selectedWorktreeId)
        }
        .toolbar {
            ToolbarItem {
                Button {
                    showAddRepo = true
                } label: {
                    Label("Add Repository", systemImage: "folder.badge.plus")
                }
            }
        }
        .sheet(isPresented: $showAddRepo) {
            AddRepoSheet()
        }
        .onAppear {
            if selectedWorktreeId == nil {
                selectedWorktreeId = store.lastSelectedWorktreeId
            }
        }
        .onChange(of: selectedWorktreeId) { _, new in
            store.lastSelectedWorktreeId = new
            try? store.save()
        }
    }
}
```

- [ ] **Step 3: 수동 테스트**

Run: ⌘R → toolbar "Add Repository" 클릭 → 폴더 선택 → my-flutter-app 선택 → 기존 worktree들이 리스트로 보임 → 몇 개 선택 → Add → 사이드바에 노출 → 클릭 시 터미널 패널에 셸 프롬프트가 떠야 함.

- [ ] **Step 4: Commit**

```bash
git add Parallel/Views/Sheets/AddRepoSheet.swift Parallel/Views/ContentView.swift
git commit -m "feat(views): AddRepoSheet with worktree discovery and import"
```

---

## Task 15: NewWorktreeSheet — worktree 생성

**Files:**
- Create: `Parallel/Views/Sheets/NewWorktreeSheet.swift`
- Modify: `Parallel/Views/ContentView.swift` (toolbar 버튼 + sheet)

- [ ] **Step 1: NewWorktreeSheet 구현**

`Views/Sheets/NewWorktreeSheet.swift`:
```swift
import SwiftUI

struct NewWorktreeSheet: View {
    @Environment(WorkspaceStore.self) private var store
    @Environment(SessionManager.self) private var sessionManager
    @Environment(\.dismiss) private var dismiss

    @State private var selectedRepoId: UUID?
    @State private var branch = ""
    @State private var base = "main"
    @State private var createBranch = true
    @State private var setupCommands = ""
    @State private var displayName = ""
    @State private var errorMessage: String?

    private let svc = WorktreeService()

    var body: some View {
        VStack(alignment: .leading, spacing: 10) {
            Text("New Worktree").font(.title2).bold()

            Picker("Repository", selection: $selectedRepoId) {
                Text("(select)").tag(UUID?.none)
                ForEach(store.repos) { r in
                    Text(r.displayName).tag(UUID?.some(r.id))
                }
            }
            .onChange(of: selectedRepoId) { _, _ in prefillSetup() }

            HStack {
                TextField("Branch", text: $branch)
                    .onChange(of: branch) { _, _ in displayName = sanitizedName }
                Toggle("New branch", isOn: $createBranch)
            }
            TextField("Base", text: $base).disabled(!createBranch)
            TextField("Display name", text: $displayName)

            if let preview = pathPreview {
                Text("Path: \(preview)").font(.caption).foregroundStyle(.secondary)
            }

            TextField("Setup commands (one per line)",
                      text: $setupCommands, axis: .vertical)
                .lineLimit(3, reservesSpace: true)

            if let errorMessage {
                Text(errorMessage).foregroundStyle(.red)
            }

            HStack {
                Spacer()
                Button("Cancel") { dismiss() }
                Button("Create") { commit() }
                    .keyboardShortcut(.defaultAction)
                    .disabled(selectedRepoId == nil || branch.isEmpty)
            }
        }
        .padding(20)
        .frame(width: 460)
    }

    private var selectedRepo: Repo? {
        guard let id = selectedRepoId else { return nil }
        return store.repos.first { $0.id == id }
    }

    private var sanitizedName: String {
        PathSanitizer.sanitize(branch)
    }

    private var pathPreview: String? {
        guard let repo = selectedRepo else { return nil }
        let raw = repo.root.appendingPathComponent(repo.worktreeBaseDir)
            .appendingPathComponent(sanitizedName)
        return uniquePath(base: raw).path
    }

    private func prefillSetup() {
        if setupCommands.isEmpty, let repo = selectedRepo {
            setupCommands = repo.defaultSetupCommands.joined(separator: "\n")
        }
    }

    private func uniquePath(base: URL) -> URL {
        if !FileManager.default.fileExists(atPath: base.path) { return base }
        var n = 2
        while true {
            let candidate = base.deletingLastPathComponent()
                .appendingPathComponent("\(base.lastPathComponent)-\(n)")
            if !FileManager.default.fileExists(atPath: candidate.path) { return candidate }
            n += 1
        }
    }

    private func commit() {
        guard let repo = selectedRepo else { return }
        let raw = repo.root.appendingPathComponent(repo.worktreeBaseDir)
            .appendingPathComponent(sanitizedName)
        let path = uniquePath(base: raw)
        let lines = setupCommands.split(separator: "\n")
            .map { String($0).trimmingCharacters(in: .whitespaces) }
            .filter { !$0.isEmpty }
        do {
            try svc.add(repoRoot: repo.root, branch: branch, base: base,
                        path: path, createBranch: createBranch)
            let wt = Worktree(repoId: repo.id, path: path, branch: branch,
                              displayName: displayName.isEmpty ? sanitizedName : displayName,
                              setupCommands: lines)
            store.addWorktree(wt)
            sessionManager.ensureSession(for: wt, setupCommands: lines)
            dismiss()
        } catch {
            errorMessage = error.localizedDescription
        }
    }
}
```

- [ ] **Step 2: ContentView toolbar 확장**

`Views/ContentView.swift` toolbar에 버튼 추가:
```swift
.toolbar {
    ToolbarItem {
        Button { showAddRepo = true } label: {
            Label("Add Repository", systemImage: "folder.badge.plus")
        }
    }
    ToolbarItem {
        Button { showNewWorktree = true } label: {
            Label("New Worktree", systemImage: "plus.square.on.square")
        }
        .disabled(store.repos.isEmpty)
    }
}
.sheet(isPresented: $showAddRepo) { AddRepoSheet() }
.sheet(isPresented: $showNewWorktree) { NewWorktreeSheet() }
```

위에 state도 추가:
```swift
@State private var showNewWorktree = false
```

- [ ] **Step 3: 수동 테스트**

Run: ⌘R → "New Worktree" → repo 선택 → branch="feat/test1" → 경로 미리보기 확인 → Create → 사이드바에 나타나고 터미널 패널에 셸 뜸. `feat-test1` 디렉토리에서 setup 명령이 자동 입력됐는지 확인.

- [ ] **Step 4: Commit**

```bash
git add Parallel/Views/Sheets/NewWorktreeSheet.swift Parallel/Views/ContentView.swift
git commit -m "feat(views): NewWorktreeSheet with path preview + setup prefill"
```

---

## Task 16: StatusWatcher — 5초 폴링, 동시 한도 4, 창 active일 때만

**Files:**
- Create: `Parallel/Services/StatusWatcher.swift`
- Modify: `Parallel/Persistence/WorkspaceStore.swift` (statuses dict 추가)
- Modify: `Parallel/ParallelApp.swift` (StatusWatcher 주입·시작)

- [ ] **Step 1: WorkspaceStore에 statuses 추가**

`WorkspaceStore.swift`에 추가:
```swift
// 영속화 X — 메모리만
var statuses: [UUID: WorktreeStatus] = [:]
```

- [ ] **Step 2: StatusWatcher 구현**

`Parallel/Services/StatusWatcher.swift`:
```swift
import Foundation
import AppKit
import Observation

@Observable
final class StatusWatcher {
    private let store: WorkspaceStore
    private let svc = WorktreeService()
    private var timer: Timer?
    private let semaphore = DispatchSemaphore(value: 4)
    private let queue = DispatchQueue(label: "parallel.statuswatcher", attributes: .concurrent)

    init(store: WorkspaceStore) {
        self.store = store
    }

    func start() {
        stop()
        let t = Timer(timeInterval: 5, repeats: true) { [weak self] _ in
            self?.tick()
        }
        RunLoop.main.add(t, forMode: .common)
        self.timer = t
        NotificationCenter.default.addObserver(
            self, selector: #selector(appActive),
            name: NSApplication.didBecomeActiveNotification, object: nil)
        NotificationCenter.default.addObserver(
            self, selector: #selector(appInactive),
            name: NSApplication.didResignActiveNotification, object: nil)
        // 첫 회 즉시
        tick()
    }

    func stop() {
        timer?.invalidate()
        timer = nil
    }

    @objc private func appActive() { start() }
    @objc private func appInactive() { stop() }

    private func tick() {
        let snapshot = store.worktrees
        for wt in snapshot {
            queue.async { [weak self] in
                guard let self else { return }
                self.semaphore.wait()
                defer { self.semaphore.signal() }
                do {
                    let s = try self.svc.status(at: wt.path)
                    DispatchQueue.main.async {
                        self.store.statuses[wt.id] = s
                    }
                } catch {
                    DispatchQueue.main.async {
                        var s = self.store.statuses[wt.id] ?? WorktreeStatus()
                        s.lastError = error.localizedDescription
                        s.lastCheckedAt = Date()
                        self.store.statuses[wt.id] = s
                    }
                }
            }
        }
    }
}
```

- [ ] **Step 3: App 진입점에서 시작**

`ParallelApp.swift`:
```swift
@main
struct ParallelApp: App {
    @State private var store: WorkspaceStore = {
        let s = WorkspaceStore(directory: WorkspaceStore.defaultDirectory)
        try? s.load()
        return s
    }()
    @State private var sessionManager = SessionManager()
    @State private var statusWatcher: StatusWatcher?

    var body: some Scene {
        WindowGroup("Parallel") {
            ContentView()
                .frame(minWidth: 900, minHeight: 600)
                .environment(store)
                .environment(sessionManager)
                .onAppear {
                    if statusWatcher == nil {
                        let w = StatusWatcher(store: store)
                        statusWatcher = w
                        w.start()
                    }
                }
        }
    }
}
```

- [ ] **Step 4: 수동 확인**

Run: ⌘R → 등록된 worktree 중 하나에서 파일 수정 → 5초 안에 store.statuses에 isDirty=true 반영되는지 (콘솔 로그로 확인하거나 다음 task의 배지로).

- [ ] **Step 5: Commit**

```bash
git add Parallel/Services/StatusWatcher.swift Parallel/Persistence/WorkspaceStore.swift Parallel/ParallelApp.swift
git commit -m "feat(services): StatusWatcher 5s polling with concurrency cap 4"
```

---

## Task 17: 사이드바 상태 배지

**Files:**
- Modify: `Parallel/Views/SidebarView.swift`

- [ ] **Step 1: WorktreeRow 확장 — 배지 표시**

`Views/SidebarView.swift`의 `WorktreeRow` 교체:
```swift
private struct WorktreeRow: View {
    @Environment(WorkspaceStore.self) private var store
    @Environment(SessionManager.self) private var sessionManager
    let worktree: Worktree

    var body: some View {
        let status = store.statuses[worktree.id]
        let session = sessionManager.session(for: worktree.id)

        HStack(spacing: 6) {
            stateDot(status: status, session: session)
            Text(worktree.displayName)
                .lineLimit(1)
            Spacer()
            if let s = status, s.lastError != nil {
                Image(systemName: "exclamationmark.triangle.fill")
                    .foregroundStyle(.orange)
                    .help(s.lastError ?? "")
            } else if let s = status, s.changedFiles > 0 {
                Text("\(s.changedFiles)")
                    .font(.caption)
                    .padding(.horizontal, 6)
                    .background(Capsule().fill(.tertiary))
            }
        }
    }

    @ViewBuilder
    private func stateDot(status: WorktreeStatus?, session: SessionManager.SessionEntry?) -> some View {
        if session == nil || (session?.session.state == .exited(code: 0)) {
            Image(systemName: "moon.zzz.fill")
                .font(.caption2)
                .foregroundStyle(.secondary)
        } else if status?.isDirty == true {
            Circle().fill(.orange).frame(width: 8, height: 8)
        } else {
            Circle().fill(.green).frame(width: 8, height: 8)
        }
    }
}
```

- [ ] **Step 2: 수동 확인**

Run: ⌘R → 깨끗한 worktree는 ● green, 파일 수정하면 ● orange + 변경파일 수 배지, 세션 종료(`exit`)하면 💤 아이콘.

- [ ] **Step 3: Commit**

```bash
git add Parallel/Views/SidebarView.swift
git commit -m "feat(views): worktree row badges (state dot, change count, error)"
```

---

## Task 18: 죽은 세션 placeholder + Restart 버튼

**Files:**
- Modify: `Parallel/Views/TerminalPaneView.swift`

- [ ] **Step 1: TerminalPaneView 분기 추가**

`Views/TerminalPaneView.swift` 교체:
```swift
import SwiftUI
import SwiftTerm
import AppKit

struct TerminalPaneView: View {
    @Environment(WorkspaceStore.self) private var store
    @Environment(SessionManager.self) private var sessionManager
    let worktreeId: UUID?

    var body: some View {
        Group {
            if let id = worktreeId, let wt = store.worktree(id: id) {
                let entry = sessionManager.session(for: wt.id)
                if let entry, case .running = entry.session.state {
                    TerminalContainer(worktree: wt)
                        .id(wt.id)
                } else {
                    deadSessionPlaceholder(for: wt)
                }
            } else {
                emptyPlaceholder
            }
        }
    }

    private var emptyPlaceholder: some View {
        VStack(spacing: 8) {
            Image(systemName: "terminal").font(.system(size: 40)).foregroundStyle(.tertiary)
            Text("Select a worktree").foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }

    private func deadSessionPlaceholder(for wt: Worktree) -> some View {
        VStack(spacing: 12) {
            Image(systemName: "moon.zzz.fill").font(.system(size: 40)).foregroundStyle(.tertiary)
            Text("Session ended").font(.headline)
            Text(wt.path.path).font(.caption).foregroundStyle(.secondary)
            Button("Restart Session") {
                sessionManager.restartSession(for: wt, setupCommands: wt.setupCommands)
            }
            .keyboardShortcut(.defaultAction)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

private struct TerminalContainer: NSViewRepresentable {
    @Environment(SessionManager.self) private var sessionManager
    let worktree: Worktree

    func makeNSView(context: Context) -> TerminalView {
        let entry = sessionManager.ensureSession(for: worktree, setupCommands: worktree.setupCommands)
        return entry?.terminalView ?? TerminalView(frame: .zero)
    }

    func updateNSView(_ nsView: TerminalView, context: Context) {}
}
```

- [ ] **Step 2: 수동 확인**

Run: ⌘R → 터미널에서 `exit` → placeholder + "Restart Session" → 클릭 → 새 셸이 다시 뜸.

- [ ] **Step 3: Commit**

```bash
git add Parallel/Views/TerminalPaneView.swift
git commit -m "feat(views): dead session placeholder with Restart button"
```

---

## Task 19: 키보드 단축키 + worktree 삭제 메뉴

**Files:**
- Modify: `Parallel/Views/ContentView.swift`
- Modify: `Parallel/Views/SidebarView.swift`
- Create: `Parallel/Views/Commands.swift`

- [ ] **Step 1: App 명령 정의**

`Views/Commands.swift`:
```swift
import SwiftUI

struct ParallelCommands: Commands {
    @FocusedBinding(\.contentActions) var actions

    var body: some Commands {
        CommandGroup(replacing: .newItem) {
            Button("New Worktree…") { actions?.newWorktree() }
                .keyboardShortcut("n", modifiers: .command)
            Button("Add Repository…") { actions?.addRepo() }
                .keyboardShortcut("o", modifiers: [.command, .shift])
        }
        CommandMenu("Worktree") {
            ForEach(1...9, id: \.self) { idx in
                Button("Switch to Worktree \(idx)") { actions?.selectIndex(idx - 1) }
                    .keyboardShortcut(KeyEquivalent(Character("\(idx)")), modifiers: .command)
            }
            Divider()
            Button("Close Session") { actions?.closeCurrentSession() }
                .keyboardShortcut("w", modifiers: .command)
            Button("Delete Worktree") { actions?.deleteCurrentWorktree() }
                .keyboardShortcut(.delete, modifiers: [.command, .shift])
        }
    }
}

struct ContentActions {
    var newWorktree: () -> Void = {}
    var addRepo: () -> Void = {}
    var selectIndex: (Int) -> Void = { _ in }
    var closeCurrentSession: () -> Void = {}
    var deleteCurrentWorktree: () -> Void = {}
}

private struct ContentActionsKey: FocusedValueKey {
    typealias Value = ContentActions
}
extension FocusedValues {
    var contentActions: ContentActions? {
        get { self[ContentActionsKey.self] }
        set { self[ContentActionsKey.self] = newValue }
    }
}
```

- [ ] **Step 2: ContentView에 actions 노출**

`Views/ContentView.swift`에 actions·핸들러 추가, view에 `.focusedValue` modifier:
```swift
struct ContentView: View {
    @Environment(WorkspaceStore.self) private var store
    @Environment(SessionManager.self) private var sessionManager
    @State private var selectedWorktreeId: UUID?
    @State private var showAddRepo = false
    @State private var showNewWorktree = false
    @State private var pendingDeleteId: UUID?

    var body: some View {
        NavigationSplitView {
            SidebarView(selection: $selectedWorktreeId)
                .navigationSplitViewColumnWidth(min: 200, ideal: 240, max: 320)
        } detail: {
            TerminalPaneView(worktreeId: selectedWorktreeId)
        }
        .toolbar { /* 기존 toolbar */ }
        .sheet(isPresented: $showAddRepo) { AddRepoSheet() }
        .sheet(isPresented: $showNewWorktree) { NewWorktreeSheet() }
        .alert("Delete worktree?", isPresented: Binding(
            get: { pendingDeleteId != nil },
            set: { if !$0 { pendingDeleteId = nil } }
        )) {
            Button("Delete", role: .destructive) { confirmDelete() }
            Button("Cancel", role: .cancel) {}
        } message: {
            if let id = pendingDeleteId, let wt = store.worktree(id: id) {
                Text("This will remove the worktree at \(wt.path.lastPathComponent). The branch is preserved.")
            }
        }
        .focusedValue(\.contentActions, ContentActions(
            newWorktree: { showNewWorktree = true },
            addRepo:     { showAddRepo = true },
            selectIndex: { idx in
                if idx < store.worktrees.count {
                    selectedWorktreeId = store.worktrees[idx].id
                }
            },
            closeCurrentSession: {
                if let id = selectedWorktreeId {
                    sessionManager.terminate(worktreeId: id)
                }
            },
            deleteCurrentWorktree: {
                pendingDeleteId = selectedWorktreeId
            }
        ))
        .onAppear {
            if selectedWorktreeId == nil {
                selectedWorktreeId = store.lastSelectedWorktreeId
            }
        }
        .onChange(of: selectedWorktreeId) { _, new in
            store.lastSelectedWorktreeId = new
            try? store.save()
        }
    }

    private func confirmDelete() {
        guard let id = pendingDeleteId, let wt = store.worktree(id: id),
              let repo = store.repos.first(where: { $0.id == wt.repoId }) else { return }
        sessionManager.terminate(worktreeId: id)
        do {
            try WorktreeService().remove(repoRoot: repo.root, path: wt.path, force: false)
            store.removeWorktree(id: id)
        } catch {
            // 백업으로 force 시도
            try? WorktreeService().remove(repoRoot: repo.root, path: wt.path, force: true)
            store.removeWorktree(id: id)
        }
        pendingDeleteId = nil
    }
}
```

- [ ] **Step 3: App에 Commands 추가**

`ParallelApp.swift`:
```swift
var body: some Scene {
    WindowGroup("Parallel") {
        ContentView()
            ...
    }
    .commands {
        ParallelCommands()
    }
}
```

- [ ] **Step 4: 수동 확인**

Run: ⌘R
- `Cmd+1~9`로 worktree 전환
- `Cmd+N`으로 새 worktree
- `Cmd+W`로 세션 종료
- `Cmd+Shift+⌫`로 삭제 다이얼로그

- [ ] **Step 5: Commit**

```bash
git add Parallel/Views/Commands.swift Parallel/Views/ContentView.swift Parallel/ParallelApp.swift
git commit -m "feat(views): keyboard shortcuts + delete worktree flow"
```

---

## Task 20: AppLogger + 사용자 액션 에러 다이얼로그

**Files:**
- Create: `Parallel/Util/AppLogger.swift`
- Modify: 여러 곳에서 `print` 또는 `// TODO log`를 `AppLogger.x.error(...)`로 교체

- [ ] **Step 1: AppLogger 구현**

`Parallel/Util/AppLogger.swift`:
```swift
import Foundation
import os

enum AppLogger {
    static let app      = Logger(subsystem: "com.byeonghopark.parallel", category: "app")
    static let store    = Logger(subsystem: "com.byeonghopark.parallel", category: "store")
    static let worktree = Logger(subsystem: "com.byeonghopark.parallel", category: "worktree")
    static let session  = Logger(subsystem: "com.byeonghopark.parallel", category: "session")
    static let status   = Logger(subsystem: "com.byeonghopark.parallel", category: "status")

    /// 디버그 빌드일 때만 파일 로그.
    static func bootstrapFileLogging() {
        #if DEBUG
        let dir = FileManager.default.urls(for: .libraryDirectory, in: .userDomainMask)[0]
            .appendingPathComponent("Logs/Parallel", isDirectory: true)
        try? FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
        // os.Logger는 자동으로 Console.app에 흐름.
        // 추가 파일 싱크는 향후 확장. (MVP에선 디렉토리만 보장)
        app.info("file logging dir: \(dir.path, privacy: .public)")
        #endif
    }
}
```

- [ ] **Step 2: ParallelApp init에서 bootstrap**

```swift
@main
struct ParallelApp: App {
    init() {
        AppLogger.bootstrapFileLogging()
    }
    // 나머지 동일
}
```

- [ ] **Step 3: WorkspaceStore 에러 로깅**

`WorkspaceStore.swift`의 `load()` 손상 격리 부분에 추가:
```swift
} catch {
    AppLogger.store.error("workspace.json corrupt: \(error.localizedDescription, privacy: .public)")
    // 손상 격리 (기존 로직)
    ...
}
```

- [ ] **Step 4: StatusWatcher 에러 로깅**

`StatusWatcher.swift` catch 블록:
```swift
AppLogger.status.error("status failed for \(wt.path.path, privacy: .public): \(error.localizedDescription, privacy: .public)")
```

- [ ] **Step 5: 사용자 액션 실패는 sheet 안에 표시되도록 이미 처리됨**

(AddRepoSheet·NewWorktreeSheet의 `errorMessage` 필드. 추가 작업 없음.)

- [ ] **Step 6: 빌드 확인 + Commit**

Run: ⌘B
Expected: 성공.

```bash
git add Parallel/Util/AppLogger.swift Parallel/ParallelApp.swift Parallel/Persistence/WorkspaceStore.swift Parallel/Services/StatusWatcher.swift
git commit -m "feat(util): AppLogger via os.Logger + service error logging"
```

---

## Task 21: Onboarding 한 줄 안내 + 종료 시 cleanup

**Files:**
- Modify: `Parallel/Views/ContentView.swift`
- Modify: `Parallel/ParallelApp.swift`

- [ ] **Step 1: 첫 실행 안내 (사이드바·디테일 비어있을 때)**

이미 SidebarView·TerminalPaneView placeholder에 표시 중. 다만 "세션 영속성 한계" 안내 1줄을 emptyPlaceholder에 추가:

`TerminalPaneView.swift`의 `emptyPlaceholder`:
```swift
private var emptyPlaceholder: some View {
    VStack(spacing: 8) {
        Image(systemName: "terminal").font(.system(size: 40)).foregroundStyle(.tertiary)
        Text("Select a worktree").foregroundStyle(.secondary)
        Text("Note: terminal sessions don't persist across app restarts.")
            .font(.caption)
            .foregroundStyle(.tertiary)
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
}
```

- [ ] **Step 2: 종료 시 세션 정리**

`ParallelApp.swift` body에 종료 옵저버:
```swift
var body: some Scene {
    WindowGroup("Parallel") {
        ContentView()
            ...
            .onReceive(NotificationCenter.default.publisher(for: NSApplication.willTerminateNotification)) { _ in
                for wt in store.worktrees {
                    sessionManager.terminate(worktreeId: wt.id)
                }
                AppLogger.app.info("app terminating, sessions cleaned up")
            }
    }
    .commands { ParallelCommands() }
}
```

- [ ] **Step 3: 수동 확인**

Run: ⌘R → 종료 → 다음 실행 시 worktree 목록 그대로, 세션은 모두 💤 상태 → Restart 가능.

- [ ] **Step 4: Commit**

```bash
git add Parallel/Views/TerminalPaneView.swift Parallel/ParallelApp.swift
git commit -m "chore(ux): persistence note + graceful session cleanup on exit"
```

---

## Task 22: 최종 수동 회귀 + README

**Files:**
- Create: `README.md`

- [ ] **Step 1: 시나리오 회귀 (수동)**

스펙 §8 시나리오 A~E 한 바퀴:
1. Add Repository → my-flutter-app → 기존 worktree 몇 개 import
2. New Worktree → 새 브랜치 → setup 명령 prefill 확인 → 생성 후 자동 실행
3. 사이드바에서 다른 worktree로 전환
4. 깨끗한 worktree에서 파일 수정 → 5초 내 사이드바 배지 변화
5. 터미널에서 `exit` → 💤 + Restart 동작
6. Cmd+Shift+⌫ → 삭제 확인 → worktree 제거됨
7. 앱 종료 → 재실행 → 목록 그대로, 세션은 모두 💤
8. workspace.json 망가뜨려보기 → 격리 + 빈 시작

- [ ] **Step 2: README 작성**

`README.md`:
```markdown
# Parallel

Lightweight macOS app for managing git worktrees with per-worktree PTY sessions. Built because Conductor isn't usable on git enterprise.

## Status
MVP. Personal use.

## Build
- Xcode 16+, macOS 14+
- Open `Parallel.xcodeproj`, ⌘R

## Storage
`~/Library/Application Support/Parallel/workspace.json`

## Logs
- `Console.app` filter subsystem `com.byeonghopark.parallel`
- Debug builds: `~/Library/Logs/Parallel/`

## Limits (intentional)
- Sessions don't persist across app restart (PTY re-forks)
- No AI integration; just hosts whatever you run in PTY
- No PR/diff UI

See `docs/superpowers/specs/2026-05-27-parallel-design.md` for full design.
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: README for Parallel MVP"
```

---

## Self-Review

(이 섹션은 plan 작성자가 작성 직후 셀프 체크한 결과 — 실행자는 무시 가능)

**Spec coverage** — 각 spec 섹션 → 해당 task:
- §2 Tech stack → Task 1
- §3 App shape → Task 11-13
- §4 Architecture / 컴포넌트 → Task 1, 3, 4, 5-8, 9-10, 11-13
- §5 Data model → Task 3
- §6 Directory convention → Task 2(PathSanitizer), Task 15(uniquePath)
- §7 PTY → Task 9, 10
- §8 Scenarios → Task 14(E), 15(A), 11(B), 16(C), 19(D)
- §9 Defaults #1~12 → 모두 매핑됨
- §10 Persistence → Task 4
- §11 Effort → 페이즈 매핑 일치
- §12 Open Questions → MVP 범위 외, 다루지 않음 ✓

**Type consistency** — `Repo.worktreeBaseDir`(String), `Worktree.setupCommands`([String]), `SessionEntry.pendingSetupCommands`([String]) 일관됨. `WorktreeStatus`는 영속화 X, store.statuses dict로만 보유 — 일관됨. `SessionManager.session(for:)` vs `ensureSession(for:)` 명확히 구분됨.

**Placeholder scan** — 없음 ✓

**한 가지 약점 인지** — Task 9의 PTY는 SwiftTerm의 `LocalProcessTerminalView` 대신 직접 구현. SwiftTerm 0.4+ API에 맞춰 작은 조정 필요할 수 있음 (특히 `TerminalViewDelegate`의 정확한 메서드 시그니처). 실행 시 컴파일 에러 나면 SwiftTerm 헤더 참고하여 시그니처 맞추면 됨.

---

## 끝.
